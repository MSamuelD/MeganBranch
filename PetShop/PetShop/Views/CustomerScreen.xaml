<Page x:Class="PetShop.Views.CustomerScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="CustomerScreen"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <!-- logo -->
        <StackPanel Grid.Row="0" Margin="0,20,0,10" HorizontalAlignment="Center">
            <Image Source="/image/logo.png" Width="200" Height="120" Stretch="Uniform"/>
            <TextBlock Text="Welcome to PetShop"
                       FontSize="10" 
                       HorizontalAlignment="Center" Margin="0,0,0,0"/>
        </StackPanel>

        <!-- button -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" >
            <Button Content="Book Appointment" Height="40" Margin="0,5" Click="BookAppointment_Click"/>
            <Button Content="Service Progress" Height="40" Margin="0,5" Click="ServiceProgress_Click"/>
            <Button Content="Weather Screen" Height="40" Margin="0,5" Click="WeatherScreen_Click"/>
        </StackPanel>
    </Grid>
</Page>

