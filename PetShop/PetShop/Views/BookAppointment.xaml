<Page x:Class="PetShop.Views.BookAppointment"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="BookAppointment"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- title -->
        <TextBlock Grid.Row="0"
                   Text="Book an Appointment"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,20"/>

        <!-- main action -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Width="400"
                    Margin="0,10,0,0">

            <!-- date -->
            <TextBlock Text="Select Date:" FontSize="16" Margin="0,0,0,4"/>
            <DatePicker x:Name="AppointmentDatePicker" Width="220" Height="28" Margin="0,0,0,12"/>

            <!-- time -->
            <TextBlock Text="Pick a Time:" FontSize="16" Margin="0,0,0,4"/>
            <ComboBox x:Name="TimeSlotBox"
                      DisplayMemberPath="TimeFormatted"
                      Width="220" Height="28"
                      Margin="0,0,0,12"/>

            <!-- pet name -->
            <TextBlock Text="Pet Name:" FontSize="16" Margin="0,0,0,4"/>
            <TextBox x:Name="PetNameBox" Width="220" Height="28" Margin="0,0,0,12"/>

            <!-- customer name -->
            <TextBlock Text="Customer Name:" FontSize="16" Margin="0,0,0,4"/>
            <TextBox x:Name="CustomerNameBox" Width="220" Height="28" Margin="0,0,0,12"/>

            <!-- service, free text -->
            <TextBlock Text="Service:" FontSize="16" Margin="0,0,0,4"/>
            <TextBox x:Name="DetailsBox"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="60"
                     Width="220"
                     SpellCheck.IsEnabled="True"
                     Margin="0,0,0,12"/>

            <!-- button -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button x:Name="SubmitBtn"
                        Content="Submit"
                        Width="100" Height="32"
                        Margin="0,0,12,0"
                        Click="SubmitBtn_Click"/>
                <Button x:Name="CancelBtn"
                        Content="Cancel"
                        Width="100" Height="32"
                        Click="CancelBtn1_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
