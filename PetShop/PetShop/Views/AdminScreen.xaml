<Page x:Class="PetShop.Views.AdminScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="AdminScreen"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- logo -->
        <StackPanel Grid.Row="0" Margin="0,20,0,10" HorizontalAlignment="Center">
            <Image Source="/image/logo.png" Width="200" Height="120"/>
            <TextBlock Text="Admin Dashboard"
                       FontSize="10" 
                       HorizontalAlignment="Center" Margin="0,0,0,0"/>
        </StackPanel>

        <!-- button -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <Button Content="Customer Options" Height="40" Margin="0,5" Click="CustomerOptions_Click"/>
            <Button Content="Staff Options" Height="40" Margin="0,5" Click="StaffOptions_Click"/>
            <Button Content="Weather Screen" Height="40" Margin="0,5" Click="WeatherScreen_Click"/>
            <Button Content="Booking Statistics" Height="40" Margin="0,5" Click="BookingStatistics_Click"/>
            <Button Content="Pet" Height="40" Margin="0,5" Click="Pet_Click"/>
        </StackPanel>
    </Grid>
</Page>